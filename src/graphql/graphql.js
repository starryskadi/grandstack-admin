// TODO: Import Apollo Client
// TODO: Generate GQL Tag and Return with functions

export const searchFields = (type, field) => {
  // Type to Search
  // Field to Search
};

export const readField = type => {
  // Type to Read
  // All Fields
};

export const createField = (type, fields) => {
  // Type to Create
  // Fields - To Input Data
};

export const updateField = (type, fields) => {
  // Type to Update
  // Fields - To Input Data
};

export const addField = () => {
  // Type to add field
  // Fields - To Input Data
};

export const removeField = () => {
  // Type to remove field
  // Fields - To Input Data
};
